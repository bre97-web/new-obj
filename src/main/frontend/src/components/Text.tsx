
export const Display = ({ msg }: { msg: string }) => (
    <h1 class="text-4xl font-bold">
        { msg }
    </h1>
)

export const Headline = ({ msg }: { msg: string }) => (
    <h1 class="text-2xl font-semibold">
        { msg }
    </h1>
)
